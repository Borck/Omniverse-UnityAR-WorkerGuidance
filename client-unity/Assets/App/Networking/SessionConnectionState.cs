namespace Guidance.Runtime
{
    /// <summary>
    /// Session connectivity state surfaced to runtime and HUD.
    /// </summary>
    public enum SessionConnectionState
    {
        Disconnected,
        Connected,
        Faulted
    }
}
